Read every document under /docs in numerical order.

Treat them as the single source of truth.

Build the application from scratch.

Do not skip phases.

Complete one phase before moving to the next.

Mark completed tasks in a running checklist.

Do not invent features outside the specification.

If an ambiguity exists, implement the simplest production-ready solution.

The application should always remain in a buildable state.